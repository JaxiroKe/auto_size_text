name: auto_size_text
description: Flutter widget that automatically resizes text to fit perfectly within its bounds.
version: 3.0.0
homepage: https://github.com/Oyonde/auto_size_text

environment:
  sdk: '>=2.19.1 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  pedantic: '>=1.11.1 <3.0.0'
